# Endee Vector DB
ENDEE_HOST=http://localhost:8080
ENDEE_TOKEN=                    # Leave empty for no-auth mode

# OpenAI (optional — for LLM-powered answers)
# If not set, the system uses a rule-based fallback answer
OPENAI_API_KEY=
